import { CheckboxdirectiveDirective } from './checkboxdirective.directive';

describe('CheckboxdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckboxdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
